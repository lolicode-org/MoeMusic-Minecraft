# Gradle JVM / config
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Required at settings phase (before version catalog is available)
loom_version=1.16-SNAPSHOT
